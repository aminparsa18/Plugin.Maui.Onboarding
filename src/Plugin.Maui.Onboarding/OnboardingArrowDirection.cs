namespace Plugin.Maui.Onboarding;

/// <summary>Which way the tooltip arrow's tip points — always back toward the spotlight target, the
/// opposite side from wherever <c>OnboardingGeometryMath.ChoosePlacement</c> put the tooltip.</summary>
public enum OnboardingArrowDirection
{
    Up,
    Down,
    Left,
    Right
}
